# @format

services:
  - type: web
    name: loan-risk-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api_service:app --host 0.0.0.0 --port 10000"
    plan: free
